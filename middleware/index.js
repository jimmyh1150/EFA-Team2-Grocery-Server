module.exports = {

    validateSession: require('./validate-session'), 
    headers: require('./headers') 
}

    validateSession: require('./validate-session'),
    headers: require('./headers')
}

